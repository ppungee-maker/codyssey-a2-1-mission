# [Project A] 브랜드 아이덴티티 생성기

> 코디세이 `AI 활용 학습 (AI Native Advanced)` 과정 · `codyssey mission` API로 원문 그대로 발췌.

### 1. 미션 소개

브랜드 디자인 외주비가 수백만 원에서 시작하는 이유가 있습니다. 네이밍, 슬로건, 컬러, 로고까지 하나하나가 전문 작업이기 때문입니다.

그 작업을 브리프 하나로 자동화할 수 있다면 어떨까요.

LLM API로 텍스트 브랜드 요소를 생성하고, 이미지 생성 API로 로고 시안까지 뽑는 CLI 프로그램을 완성합니다.

브랜드 디자인은 네이밍, 슬로건, 스토리, 컬러, 로고 등 다양한 요소를 종합적으로 기획해야 하는 작업입니다. 외주 플랫폼에서 브랜드 디자인 외주가 활발한 이유도 이 작업이 시간과 전문성을 요구하기 때문입니다.

이 미션에서는 브랜드 브리프(업종, 타겟, 키워드 등)를 입력하면 AI가 브랜드 네이밍, 슬로건, 스토리, 컬러 팔레트, 로고 시안까지 자동 생성하는 CLI 프로그램을 구현합니다.

LLM API로 텍스트 기반 브랜드 요소를 생성하고, 이미지 생성 API로 로고 시안을 만든 뒤, 모든 결과물을 폴더에 저장합니다.

### 2. 최종 결과물

다음 기능이 정상 동작하는 CLI 기반 Python 프로그램 1개를 완성한다.

1. 브랜드 브리프 입력 — JSON 파일로 브랜드 정보(업종, 타겟, 키워드, 톤앤매너 등)를 입력받는다.
2. AI 기반 브랜드 요소 생성 — 브랜드 네이밍 후보 3~5개와 각각의 의미, 슬로건/태그라인 3개, 브랜드 스토리(탄생 배경, 철학), 브랜드에 어울리는 컬러 팔레트(메인/서브 컬러)
3. AI 기반 로고 시안 생성 — 이미지 생성 API를 활용하여 로고 시안 2~3개를 PNG 파일로 저장
4. 결과 저장 — 텍스트 결과를 JSON 파일로 저장, 컬러 팔레트를 시각화하여 PNG로 저장, 로고 시안을 PNG로 저장

### 3. 과제 목표

이 과제를 마친 후, 학습자는 아래를 스스로 설명할 수 있어야 한다.

- 브랜드 브리프를 입력받아 AI로 브랜드 요소를 생성하는 파이프라인을 설명할 수 있다.
- LLM API와 이미지 생성 API를 조합하여 텍스트+이미지 결과물을 생성하는 방법을 설명할 수 있다.
- 생성된 컬러 팔레트를 시각화하여 이미지로 저장하는 방법을 설명할 수 있다.
- API 호출 시 발생할 수 있는 오류 상황과 대응 방법을 설명할 수 있다.

### 4. 기능 요구 사항

1. **사용자 입력**: print와 input을 사용하여 대화형으로 정보를 입력받는다. 필수 입력: 브리프 파일 경로. 선택 입력: 출력 폴더 경로(기본값 `./output`).
2. **브랜드 브리프 입력**: JSON 파일로 입력. 필수 필드 `industry`/`target`/`keywords`, 선택 필드 `tone`/`competitors`/`notes`.
3. **브랜드 네이밍 생성**: LLM API로 브랜드명 후보 3~5개 + 각 의미/유래 설명.
4. **슬로건 생성**: LLM API로 슬로건/태그라인 3개, 톤앤매너에 맞는 문구.
5. **브랜드 스토리 생성**: LLM API로 300자 내외, 탄생 배경/철학/비전 포함.
6. **컬러 팔레트 생성**: 메인 컬러 1개, 서브 컬러 2~3개(HEX). matplotlib 등으로 시각화하여 PNG 저장.
7. **로고 시안 생성**: 이미지 생성 API(DALL-E 등)로 2~3개 생성, PNG로 저장.
8. **결과 저장**: 텍스트는 `brand_result.json`, 이미지는 개별 PNG.
9. **에러 처리**: API 호출 실패 시 에러 메시지 출력 후 다음 단계 계속 진행. API 키 없음/오류 시 명확한 안내.
10. **API 키 관리**: 코드에 직접 작성 금지, 환경 변수 또는 설정 파일에서 로드.

### 5. 보너스 과제 (선택)

1. 경쟁사 분석 추가 — 입력된 경쟁사 브랜드를 분석하여 차별화 포인트 제안.
2. 다국어 네이밍 지원 — 한글 네이밍과 함께 영문 네이밍 동시 생성.
